apiVersion: v1
kind: Service

metadata:
  name: project5-app
  namespace: project5

spec:
  type: ClusterIP

  selector:
    app: project5-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
